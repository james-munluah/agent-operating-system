# Normalise to LF in the repository. Authored on Windows, run in CI on Linux;
# without this the hooks ship with CRLF and a shebang line reads as
# "#!/usr/bin/env node\r", which fails with a confusing "no such file" error.
* text=auto eol=lf
*.png binary
*.jpg binary
*.pdf binary
