{
  "name": "agent-operating-system",
  "version": "1.0.0",
  "description": "Guardrails, review agents and routing for coding agents. Mechanisms, not instructions.",
  "type": "commonjs",
  "license": "MIT",
  "author": "James Munluah",
  "scripts": {
    "pretest": "node scripts/check-no-client-refs.mjs",
    "test": "node scripts/check-no-client-refs.mjs --self-test && node --test",
    "gate": "node scripts/check-no-client-refs.mjs --self-test && node scripts/check-no-client-refs.mjs",
    "demo": "node demo/review-lenses.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.123.0",
    "zod": "^4.5.4"
  }
}
